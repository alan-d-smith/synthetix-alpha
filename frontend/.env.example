# Optional public URL of the read-only Synthetix Alpha dashboard adapter.
# This value is safe to expose to the browser. Do not add provider or broker credentials here.
NEXT_PUBLIC_DASHBOARD_API_URL=http://127.0.0.1:8000
